export interface ButtonProps {
  action: () => void;
  buttonLabel: string;
  buttonBackgroundColor?: string; // optional
  buttonlabel: string;
  buttonSize: string;
}
interface LayoutProps {
  children: string;
}